# Build output — Netlify regenerates dist/ from source via netlify-build.sh
dist/

# Archivos fuente / apps de trabajo — no se publican y son muy pesados (~900MB).
# Incluye Descargas/Admin y Descargas/ControlApp, que tienen su propio .git
# (por eso rompían el deploy de Netlify como submódulos sin .gitmodules).
Descargas/

# Sistema / editor
.DS_Store
Thumbs.db
